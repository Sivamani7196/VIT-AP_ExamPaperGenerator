spring.application.name=question-paper-generator
spring.profiles.active=dev

# Default profile uses H2 from application-dev.properties.
# For MySQL, set spring.profiles.active=mysql and define datasource values in application-mysql.properties.
# Authentication is database-backed via AppUser entity.

spring.thymeleaf.cache=false
server.port=${PORT:9898}